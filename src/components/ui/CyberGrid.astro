---
// CyberGrid.astro - Wersja Cinematic Depth of Field
---

<div class="cyber-scene">
  <div class="grid-wrapper">
    <div class="grid-texture"></div>
  </div>
  
  <div class="horizon-dof"></div>
</div>

<style>
  .cyber-scene {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    perspective: 450px;
    overflow: hidden;
    pointer-events: none;
    background: transparent;

    /* ZMIANA 1: Agresywniejsza maska widoczności.
       Przesunąłem start widoczności z 30% na 50%.
       Ukrywamy "brzydki" koniec siatki całkowicie.
    */
    mask-image: linear-gradient(to bottom, 
      transparent 0%, 
      transparent 50%, 
      black 90%, 
      black 100%
    );
    -webkit-mask-image: linear-gradient(to bottom, 
      transparent 0%, 
      transparent 50%, 
      black 90%, 
      black 100%
    );
  }

  /* NOWOŚĆ: Styl dla Głębi Ostrości */
  .horizon-dof {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 60%; /* Zajmuje górne 60% ekranu */
    z-index: 10;
    
    /* MAGIA: Rozmywamy wszystko, co jest pod spodem (siatkę i wideo) */
    /* 3px to wystarczająco, żeby zabić migotanie */
    backdrop-filter: blur(3px);
    -webkit-backdrop-filter: blur(3px);

    /* Maska gradientowa dla samego blura */
    /* Góra jest mocno rozmyta (black), dół jest ostry (transparent) */
    mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
    -webkit-mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
  }

  .grid-wrapper {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transform: rotateX(80deg);
    transform-style: preserve-3d;
  }

  .grid-texture {
    position: absolute;
    top: -100%; 
    left: -100%;
    width: 300%;
    height: 300%;
    
    /* Linie: Ostre, wyraziste kolory */
    background-image: 
      /* MAJOR (Limonka) */
      linear-gradient(90deg, rgba(204, 255, 0, 0.5) 2px, transparent 2px),
      linear-gradient(180deg, rgba(204, 255, 0, 0.5) 2px, transparent 2px),

      /* MINOR (Białe) */
      linear-gradient(90deg, rgba(255, 255, 255, 0.07) 1px, transparent 1px),
      linear-gradient(180deg, rgba(255, 255, 255, 0.07) 1px, transparent 1px);

    background-size: 
      200px 200px, 
      200px 200px, 
      40px 40px, 
      40px 40px;

    /* Powolny, dostojny lot (6s) */
    animation: grid-fly 6s linear infinite;
    
    will-change: transform;
    backface-visibility: hidden;
  }

  @keyframes grid-fly {
    0% {
      transform: translateY(0);
    }
    100% {
      transform: translateY(200px);
    }
  }
</style>